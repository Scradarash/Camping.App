<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Camping.App.ViewModels"
             x:Class="Camping.App.Views.StaanplaatsDetailView"
             BackgroundColor="#80000000">

    <!-- AANPASSINGEN ZITTEN IN DEZE BORDER -->
    <Border Stroke="Gray"
            StrokeThickness="1"
            BackgroundColor="White"
            Padding="25"
            Margin="30"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            WidthRequest="500"
            HeightRequest="600">

        <Border.StrokeShape>
            <RoundRectangle CornerRadius="15"/>
        </Border.StrokeShape>

        <Grid RowDefinitions="Auto, Auto, Auto, Auto, *, Auto" RowSpacing="15">

            <!-- 1. Titel (Iets groter gemaakt) -->
            <Label Grid.Row="0" 
                   Text="{Binding Staanplaats.Name}" 
                   FontSize="26" 
                   FontAttributes="Bold" 
                   TextColor="Black"
                   HorizontalOptions="Center"/>

            <!-- 2. Periode -->
            <Label Grid.Row="1" 
                   Text="{Binding PeriodeTekst}"
                   TextColor="#416722"
                   FontSize="16"
                   HorizontalOptions="Center"/>

            <BoxView Grid.Row="2" HeightRequest="1" Color="LightGray" />

            <!-- 3. Beschrijving -->
            <Label Grid.Row="3"
                   Text="{Binding Staanplaats.Description}"
                   TextColor="Black"
                   FontSize="16"
                   LineBreakMode="WordWrap" />

            <!-- 4. Lijst (Scrollbaar gemaakt voor als het veel tekst is) -->
            <ScrollView Grid.Row="4">
                <VerticalStackLayout>
                    <Label Text="Toegestaan op dit veld:" 
                           FontAttributes="Bold" 
                           FontSize="14"
                           TextColor="Gray"
                           Margin="0,0,0,10"/>

                    <CollectionView ItemsSource="{Binding GeschikteAccommodaties}"
                                    SelectionMode="None">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Border Stroke="Transparent" 
                                        BackgroundColor="#F0F0F0" 
                                        Padding="15,10" 
                                        Margin="0,4">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="8"/>
                                    </Border.StrokeShape>
                                    <Label Text="{Binding Name}" 
                                           TextColor="#333"
                                           FontSize="16"
                                           FontAttributes="Bold"/>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </ScrollView>

            <!-- 5. Knoppen (Iets groter gemaakt) -->
            <Grid Grid.Row="5" ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,10,0,0">
                <Button Grid.Column="0" 
                        Text="Annuleer" 
                        Command="{Binding AnnuleerCommand}" 
                        BackgroundColor="Gray" 
                        TextColor="White"
                        HeightRequest="50"/>

                <Button Grid.Column="1" 
                        Text="Reserveer" 
                        Command="{Binding ReserveerCommand}" 
                        BackgroundColor="#0a5dc9" 
                        TextColor="White"
                        HeightRequest="50"/>
            </Grid>
        </Grid>
    </Border>
</ContentPage>