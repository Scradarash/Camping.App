<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Camping.App.ViewModels"
             x:Class="Camping.App.Views.StaanplaatsDetailView"
             BackgroundColor="#80000000">

    <Border Stroke="Gray"
            StrokeThickness="1"
            BackgroundColor="White"
            Padding="20"
            Margin="20, 60, 20, 60"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            WidthRequest="350">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="15"/>
        </Border.StrokeShape>

        <Grid RowDefinitions="Auto, Auto, Auto, Auto, *, Auto" RowSpacing="10">

            <!-- Titel -->
            <Label Grid.Row="0" 
                   Text="{Binding Staanplaats.Name}" 
                   FontSize="22" 
                   FontAttributes="Bold" 
                   TextColor="Black"
                   HorizontalOptions="Center"/>

            <!-- Periode -->
            <Label Grid.Row="1" 
                   Text="{Binding PeriodeTekst}"
                   TextColor="#416722"
                   FontSize="14"
                   HorizontalOptions="Center"/>

            <BoxView Grid.Row="2" HeightRequest="1" Color="LightGray" />

            <!-- Beschrijving -->
            <Label Grid.Row="3"
                   Text="{Binding Staanplaats.Description}"
                   TextColor="Black"
                   FontSize="14"
                   LineBreakMode="WordWrap" />

            <!-- Lijst met toegestane accommodaties -->
            <VerticalStackLayout Grid.Row="4">
                <Label Text="Toegestaan op dit veld:" 
                       FontAttributes="Bold" 
                       FontSize="12"
                       TextColor="Gray"
                       Margin="0,10,0,5"/>

                <CollectionView ItemsSource="{Binding GeschikteAccommodaties}"
                                SelectionMode="None">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Stroke="Transparent" 
                                    BackgroundColor="#F0F0F0" 
                                    Padding="10,5" 
                                    Margin="0,2">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="5"/>
                                </Border.StrokeShape>
                                <Label Text="{Binding Name}" 
                                       TextColor="#333"
                                       FontSize="14"/>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>

            <!-- Knoppen -->
            <Grid Grid.Row="5" ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,15,0,0">
                <Button Grid.Column="0" 
                        Text="Annuleer" 
                        Command="{Binding AnnuleerCommand}" 
                        BackgroundColor="Gray" 
                        TextColor="White"/>

                <Button Grid.Column="1" 
                        Text="Reserveer" 
                        Command="{Binding ReserveerCommand}" 
                        BackgroundColor="#0a5dc9" 
                        TextColor="White"/>
            </Grid>
        </Grid>
    </Border>
</ContentPage>